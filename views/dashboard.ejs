<!DOCTYPE html>
<html>
  <head>
    <title>Dashboard</title>
    <style>
      /* Add some basic styling to the dashboard */
      body {
        margin: 0;
        font-family: Arial, Helvetica, sans-serif;
      }

      .header {
        background-color: #333;
        color: white;
        padding: 10px;
        text-align: center;
      }

      .nav {
        display: flex;
        background-color: #f1f1f1;
        overflow: hidden;
      }

      .nav a {
        float: left;
        color: #333;
        text-align: center;
        padding: 14px 16px;
        text-decoration: none;
      }

      .nav a:hover {
        background-color: #ddd;
        color: black;
      }

      .nav a.active {
        background-color: #4caf50;
        color: white;
      }

      .main {
        padding: 20px;
      }

      .card {
        background-color: #f1f1f1;
        padding: 20px;
        margin-top: 20px;
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
        border-radius: 5px;
      }

      .card h2 {
        font-size: 24px;
        margin-bottom: 10px;
      }

      .card p {
        margin-bottom: 10px;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>Welcome to Dashboard</h1>
    </div>

    <div class="nav">
      <a href="#" class="active">Home</a>
      <% if (user.userType == "admin") { %>
      <a href="/upload">Upload File</a>
      <%}%>
      <a href="/view-notes/<%= user.semester %>">View Notes</a>
      <a href="/view-questionpapers/<%= user.semester %>">View Question Papers</a>
      <a href="/view-questionbank/<%= user.semester %>">View Question Banks</a>
      <a href="/logout" style="float:right">Logout</a>
    </div>

    <div class="main">
      <h2>User Details</h2>

      <% if (user) { %>
      <div class="card">
        <h2><%= user.name %></h2>
        <p><strong>User ID:</strong> <%= user.userId %></p>
        <% if (user.semester) { %>
        <p><strong>Semester:</strong> <%= user.semester %></p>
        <% } %>
        <p><strong>Branch:</strong> <%= user.branch %></p>
        <p><strong>User Type:</strong> <%= user.userType %></p>
      </div>
      <% } else { %>
      <p>No user details found.</p>
      <% } %>
    </div>
  </body>
</html>
